import pandas as pd


def pair_anomaly_detect(dataframe, comparison_metrics):
    '''
    对两段数据进行对比，如果指标变化超过阈值，则返回False，反之返回False
    :param dataframe: 需要冲突检测的源数据
    :param metrics: a dict:{指标：阈值}
    :return: 如果指标变化超过阈值，则返回False，反之返回False
    '''


def anomaly_detector(dataframe, comparison_metrics, screening_metrics):
    '''

    :param dataframe: 源数据
    :param comparison_metrics:
    :param screening_metrics:
    :return:
    '''


def main():
    df = pd.DataFrame()

if __name__ == '__main__':
    main()